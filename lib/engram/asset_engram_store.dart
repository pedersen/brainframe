// AssetEngramStore: read-only store over bundled assets (all platforms),
// enumerating files via AssetManifest and reading from rootBundle. Scaffolding
// placeholder — implemented in Step 3 (asset-bundle store and built-in
// engrams).
