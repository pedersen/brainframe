// EngramScope: an InheritedWidget holding the single active engram, switching
// releases the previous engram's store before resolving the next. Scaffolding
// placeholder — implemented in Step 7 (EngramScope and app startup).
