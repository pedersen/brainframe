// Conditional-export seam for the filesystem store, mirroring
// lib/window/window_state.dart: the real dart:io implementation on native
// platforms, a throwing stub on web. Scaffolding placeholder — wired up in
// Step 4 (filesystem store and location).
