// EngramLocation: the filesystem-only detail of which directory an engram
// lives in (container-default or a picked path) and how to access it.
// Scaffolding placeholder — implemented in Step 4 (filesystem store and
// location).
