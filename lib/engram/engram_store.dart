// Abstract EngramStore contract: the content-access seam every engram is
// reached through, so asset-backed built-ins and on-disk engrams sit alongside
// each other. Scaffolding placeholder — implemented in Step 2 (models and the
// store contract).
