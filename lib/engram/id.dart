// Stable engram ids. Decision: a small no-dependency Crockford-base32 ULID
// helper rather than the `ulid` package, to keep the dependency surface small.
// Scaffolding placeholder — implemented in Step 2 (models and the store
// contract).
